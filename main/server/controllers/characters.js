module.exports.getCharacters = function (req, res) {
// return the character array from the config characters json
};

module.exports.getCharacter = function (req, res) {
    var id = req.params.id;
    // returns a specific character from the config json file
};